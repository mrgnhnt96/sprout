{
  "//": [
    "CLAIMS ABOUT THINGS THIS REPO DOES NOT CONTROL, and the version each was verified against.",
    "",
    "A limitation written in prose ages exactly like a premise does, and nothing in this repo",
    "noticed. The shape that fixes it came from showrunner (its INV20): a claim CANNOT be checked",
    "automatically, but its SUBJECT can be versioned. Deciding whether 'rate_limits rides the",
    "statusline payload' is still true needs a person. Deciding whether the release it was verified",
    "against is the release running is four lines. That converts an unbounded duty -- stay vigilant",
    "forever -- into a dated one: re-read these entries on the specific day the layer moves.",
    "",
    "WHAT THIS DOES NOT DO, since that sentence is itself the kind of claim it governs: it does not",
    "check that any claim is TRUE, and it cannot. It removes the option of going stale QUIETLY.",
    "",
    "`verified_against` is null on every entry below and that is not an oversight -- it is the",
    "finding. These were read from the docs on 2026-07-29 and the version was never recorded, so",
    "there is nothing to compare and the honest report says so. The first re-verification of any",
    "entry fills it in and makes that entry checkable from then on. Do not back-fill it by guessing",
    "which version was current that day; a stamp invented today is a number standing for nothing.",
    "",
    "Each claim carries `ledger`: the exact LEDGER.md heading it accounts for. The suite",
    "matches on THOSE, not on a count -- a count passes when one entry is added and",
    "another removed, which is the shape of check that lies to you. A new VERIFIED or",
    "RULED-OUT entry about the host therefore fails the run until somebody decides it.",
    "",
    "A RUNG ABOVE VERSIONING, from showrunner: version the subject when you cannot reach it,",
    "EXERCISE the subject when you can. A stamp records that the author verified something on the",
    "author's machine and asserts it about a stranger's. An exercise checks it on the stranger's,",
    "every run, and cannot go stale because there is no recorded answer to rot. Where a fact can be",
    "provoked, the stamp is the weaker instrument -- so `exercised_by` outranks `verified_against`",
    "on any claim that carries it, and the report says which one it is speaking from.",
    "",
    "Only one of the six is exercisable today and the rest say so rather than implying otherwise:",
    "the five RULED-OUT entries are absences, and an absence cannot be provoked -- observing that",
    "no `claude usage` flag exists this run is not evidence it did not appear in the last release.",
    "",
    "THE BOUND CORRECTED, by showrunner, after I told it 'exercise a presence, version an absence'.",
    "That was generalised from a sample that agreed with itself. The discriminator is not presence",
    "versus absence -- it is whether you CONTROL THE INPUT that would produce the effect. A",
    "CONDITIONAL absence is exercisable: run the mechanism, show it fired, show the thing is still",
    "not there. That pairing turns an absence into a DISCRIMINATION. An UNCONDITIONAL absence is not,",
    "because the case where the fact is false is precisely the one you cannot construct.",
    "",
    "So `exercisable` is three-valued and the middle one is a declared debt, not a pass: 'yes-not-yet'",
    "means the control exists and nobody has written it. Saying four of these were unprovable was",
    "wrong, and it was wrong in my own tool's output, which is the failure this file exists to name.",
    "",
    "PER-CLAIM VERIFICATION, because the block stamp could not express a partial re-read. This file",
    "promised that the first re-verification would fill in the null and make THAT entry checkable.",
    "With one stamp for six claims, filling it in would have asserted six re-readings where one",
    "happened — so the promise was unkeepable as written, which is a small instance of exactly what",
    "this file exists to catch. Each claim now carries its own verified_on/verified_against, and the",
    "block values remain the original reading for entries nobody has revisited."
  ],
  "claude_code": {
    "subject": "Claude Code (the host this harness runs inside)",
    "verified_on": "2026-07-29",
    "verified_against": null,
    "why_it_matters": "the statusline tap, the limit gate and the watchdog's usage park all rest on these; if the host moved, they fail toward believing there is headroom",
    "claims": [
      {
        "id": "statusline-rate-limits",
        "says": "rate_limits.five_hour/.seven_day ride the statusline stdin JSON, each {used_percentage, resets_at}",
        "source": "https://code.claude.com/docs/en/statusline.md",
        "breaks": "game_loop statusline writes no snapshot, so limitgate and the park are inert",
        "ledger": "Claude Code exposes subscription rate limits in the statusline stdin JSON",
        "exercised_by": "limits.json carrying five_hour/seven_day with used_percentage",
        "exercise_note": "the host writes this through our own tap, so a snapshot in the expected shape IS the claim holding, observed on the consumer's machine rather than on the author's",
        "exercisable": "yes",
        "exercisable_why": "exercised: a snapshot in the expected shape IS the claim, and the missing-field arm is its positive control",
        "verified_on": "2026-08-26",
        "verified_against": "2.1.246",
        "verified_how": "RE-READ at 2.1.246, the binary actually RUNNING this session (CLAUDE_CODE_EXECPATH) rather than the 2.1.247 on PATH — two installations, and the PATH answer names the wrong subject. Every field name is present in that binary (rate_limits, five_hour, seven_day, used_percentage, resets_at), and this machine's live .game_loop/limits.json carries exactly five_hour and seven_day, each with used_percentage and resets_at. Note crossed_at and notified in that file are OUR tap's additions, not the host's."
      },
      {
        "id": "statusline-config-keys",
        "says": "statusLine takes {type: command, command, refreshInterval (min 1s), padding}",
        "source": "https://code.claude.com/docs/en/statusline.md",
        "breaks": "install.sh wires a statusline the host ignores",
        "ledger": "statusLine config keys",
        "exercisable": "no",
        "exercisable_why": "unconditional: the host either honours the keys or does not, and I cannot construct the case where it does not",
        "verified_on": "2026-08-26",
        "verified_against": "2.1.246",
        "verified_how": "PARTIAL RE-READ in the running binary: `statusLine` (29 occurrences) and `refreshInterval` (3) are present. `padding` appears 407 times and is NOT evidence — it is a common word in a 230MB binary, and counting it as confirmation would be the kind of match that agrees with whatever you hoped. So this re-read confirms two of the three key names and declines to claim the third; the docs URL remains the source for the shape."
      },
      {
        "id": "no-rate-limits-in-hooks",
        "says": "no hook payload carries rate-limit data (RULED OUT)",
        "source": "anthropics/claude-code#34817",
        "breaks": "if it became true, the whole statusline detour is unnecessary",
        "ledger": "Hook payloads carry rate-limit data",
        "exercisable": "yes",
        "exercisable_why": "CONDITIONAL absence and exercisable, not yet exercised: every hook invocation supplies a payload I control, and the payload carrying session_id is the positive control that the read worked while rate_limits was still absent",
        "verified_on": "2026-08-26",
        "verified_against": null,
        "exercised_by": "the Stop payload's own keys, scanned on every turn-end, with session_id present as the control that the read worked",
        "verified_how": "EXERCISED today: .game_loop/probe/claims-observed.json, written by a real Stop hook at 21:46, records rate_limit_keys: [] with control: True — the payload was genuine (session_id present) and carried no rate-limit key anywhere. VERSION NOT ATTRIBUTED, and that is why verified_against is null rather than 2.1.246: the hook ran with no CLAUDE_CODE_EXECPATH in its environment, so the observation cannot name the build it watched. The tool records that as an explicit null with its reason instead of inferring one."
      },
      {
        "id": "no-headless-usage-flag",
        "says": "there is no `claude usage` / --usage (RULED OUT)",
        "source": "anthropics/claude-code#44328, #40395, #39141",
        "breaks": "if it became true, usage could be read directly instead of inferred",
        "ledger": "A headless `claude usage` / `--usage` flag exists",
        "exercisable": "no",
        "exercisable_why": "unconditional: the case where the flag exists is the one I cannot construct, so there is nothing to provoke and no control to pair",
        "verified_on": "2026-08-26",
        "verified_against": "2.1.246",
        "verified_how": "RE-READ by running `--help` on the binary actually running this session. 2.1.246's subcommands are: agents, auth, auto-mode, doctor, gateway, import, install, mcp, plugin, project, setup-token, ultrareview, update. No `usage`, and no --usage flag. My first pass counted a grep for 'usage' and got 1 — that was the `Usage: claude [options]` banner, so the count was checked rather than trusted."
      },
      {
        "id": "no-readable-ratelimit-headers",
        "says": "anthropic-ratelimit-unified-* headers are not persisted anywhere scripts reach (RULED OUT)",
        "source": "anthropics/claude-code#55333",
        "breaks": "if it became true, the client-side derivative stops being the only form",
        "ledger": "`anthropic-ratelimit-unified-*` response headers are readable by harness code",
        "exercisable": "no",
        "exercisable_why": "unconditional: nothing local produces the headers, so no run can discriminate",
        "verified_on": "2026-07-29",
        "verified_against": null,
        "reverify_attempt": "2026-08-13 against 2.1.223: COULD NOT TELL, deliberately not stamped. 14 distinct anthropic-ratelimit-unified-* header names are present in the client, so the subject still exists — but the claim is about PERSISTENCE to somewhere harness code can read, and a minified binary establishes that neither way. A version stamp here would record that the header names exist, which is not what this claim says."
      },
      {
        "id": "no-weekly-opus-window",
        "says": "only five_hour and seven_day windows appear; the per-model weekly limit does not (RULED OUT)",
        "source": "https://code.claude.com/docs/en/statusline.md",
        "breaks": "nothing changes -- a run can still die unwarned on a limit the machinery cannot see",
        "ledger": "The per-model weekly (Opus) limit is in the statusline payload",
        "exercisable": "yes",
        "exercisable_why": "CONDITIONAL absence, now exercised: our own tap supplies the input, a known window present is the mechanism demonstrably firing, and an unexpected window would falsify it",
        "exercised_by": "a known window present in limits.json (the control) with no third window beside it (the claim)",
        "verified_on": "2026-08-26",
        "verified_against": "2.1.246",
        "verified_how": "PARTIAL at 2.1.246, and recorded as partial rather than as a pass. What still holds: the observed payload carries only five_hour and seven_day — that is what the claim asserts and it is unchanged. What I could NOT establish: the running binary now carries `weekly_limit` and `weekly_scoped` in its strings, and 2.1.241 is no longer on disk, so there is nothing to diff against and I cannot say whether that vocabulary is new. Absence from the PAYLOAD is the claim; absence from the CODE is a stronger thing I did not establish and may previously have implied. If a third window ever appears in a live snapshot, this is broken."
      },
      {
        "id": "fresh-session-token-floor",
        "says": "a fresh session costs ~38.4k input tokens before answering at 2.1.246, of which ~24.9k is irreducible base preamble read from cache; MCP schemas are ~1.2k and the rest is tool definitions and per-session setup",
        "source": "measured: claude -p ... --output-format json at 2.1.223",
        "breaks": "the recommendation against spawning sessions to sample usage rests on this floor; if it dropped to near zero the objection would weaken and a polling probe might be fine",
        "ledger": "A fresh Claude Code session costs ~31.5k INPUT tokens before it answers anything, and ~24k of that is irreducible",
        "verified_on": "2026-08-26",
        "verified_against": "2.1.246",
        "verified_how": "RE-MEASURED at 2.1.246 against the RUNNING binary, two probes, decomposed the same way as the original rather than taking one number:\n  baseline            38,371  (2 in · 13,444 cache-create · 24,925 cache-read)\n  --strict-mcp-config 37,139  (2 in · 12,212 cache-create · 24,925 cache-read)\nAgainst 2.1.223's 31,547 / 30,323. THE STRUCTURE HELD AND THE MAGNITUDE DID NOT: the base preamble is 24,925 against a claimed ~24k, and MCP costs 1,232 against a claimed ~1k — both intact. The total rose 6,824 (+21.6%), all of it in cache-creation, which is tool definitions and per-session setup rather than the preamble.\nDIRECTION MATTERS HERE: this claim's `breaks` says the recommendation against spawning sessions to sample usage weakens if the floor drops toward zero. It did not drop, it ROSE by a fifth, so that recommendation is stronger than when it was written. A stale number that moved in the safe direction is still stale, and the old figure would have under-stated the cost to anyone quoting it.",
        "exercisable": "yes",
        "exercisable_why": "CONDITIONAL and exercisable, and the CHEAP path is now known. Previously recorded as not-yet because the exercise spends the resource it measures and needs network and credentials. That reasoning was sound and INCOMPLETE: the statusline payload's context_window carries ABSOLUTE counts (total_input_tokens, context_window_size, current_usage.input_tokens), not only used_percentage — read out of 2.1.223's own embedded schema on 2026-08-13. So `limitprobe` already spawns a FRESH session and its render already carries this claim's number: the exercise can ride a spawn that is being paid for anyway, instead of buying one. Still a debt, but a debt with a costed route rather than a reason to leave it alone.",
        "exercised_by": "`claude -p 'reply with the single word: ok' --output-format json`, run on the binary at CLAUDE_CODE_EXECPATH, summing input_tokens + cache_creation_input_tokens + cache_read_input_tokens; repeat with --strict-mcp-config for the MCP share"
      },
      {
        "id": "statusline-context-window",
        "says": "the statusline payload's context_window carries ABSOLUTE token counts — total_input_tokens, total_output_tokens, context_window_size, and current_usage {input_tokens, output_tokens, cache_creation_input_tokens, cache_read_input_tokens} — alongside used_percentage/remaining_percentage",
        "source": "read in the running binary at 2.1.223 (the client's own embedded payload schema)",
        "breaks": "the harness reads only used_percentage, so every absolute token question is currently answered by inference or by spawning something. If these fields moved, any future usage work resting on them silently loses its measurement",
        "ledger": "The statusline payload exposes only a PERCENTAGE of context used",
        "exercisable": "yes",
        "exercisable_why": "EXERCISED 2026-08-24. Our own limit probe records the raw block to probe/context-window.json, uninterpreted, so a later reading is a real observation rather than a memory of one — which is exactly what closed it.",
        "verified_on": "2026-08-26",
        "verified_against": "2.1.246",
        "verified_how": "PARTIAL RE-READ at 2.1.246 (the running binary, not PATH's 2.1.247): total_input_tokens, total_output_tokens and context_window_size are all present. This confirms the FIELD NAMES survive the version bump; it does not re-establish that the values are absolute rather than deltas, which was the original reading's substance and was not re-measured here."
      }
    ],
    "//note": "verified_on/verified_against now live on each CLAIM. The block values are the original reading and remain as the default for entries nobody has revisited; a claim carrying its own is the one that has actually been re-read."
  },
  "saggar": {
    "subject": "saggar (the terminal manager `successor` can hand a session to)",
    "verified_on": "2026-08-27",
    "verified_against": "`saggar --help` at ~/.local/bin/saggar (2026-08-27), four live agent terminals on 2026-08-26, and ~/.saggar/claude-presence-hook.sh (rewritten 2026-08-26 11:25)",
    "why_it_matters": "`successor`'s saggar-agent mode is the handover a human is meant to find later, and what that terminal is CALLED is the whole of finding it. saggar shipped `agent --title` on 2026-08-26 and this repo spent a day documenting the gap instead of closing it — every handover opened a terminal named \"Terminal\". If the flag moves again, the handovers go unfindable and the only thing that would notice is a person scanning a row of identical terminals.",
    "claims": [
      {
        "id": "saggar-terminal-name-is-not-ours",
        "says": "BROKEN 2026-08-26 by a saggar update, on exactly the condition this entry named for itself, and CLOSED 2026-08-27 by the code catching up. `saggar agent` takes `--title <title>` and it DOES name the displayed terminal: two live agent terminals opened with a --title read back from `saggar list --json` under those exact strings, while two UNTITLED probes launched the same hour stayed at saggar's default \"Terminal\" throughout. For a day after that measurement the repo carried the correct EXPLANATION and the wrong CALL — `successor` printed \"NOT SET FROM HERE, that is OUR gap\" while `_saggar_agent` invoked the untitled form, so every handover opened a terminal called \"Terminal\". Found by the human, not by this checkout, because the test asserted stdout and the gap was in argv. `_saggar_agent` now passes `--title <title> -- <task>`, and test/run.py's fake saggar records \"$@\" so the assertion is on the argument list rather than on the paragraph describing it.",
        "source": "`saggar --help` at ~/.local/bin/saggar (agent --title, and `--` ends agent options before the task), read 2026-08-27; plus four live agent terminals read back from `saggar list --json` on 2026-08-26",
        "breaks": "What would break the CURRENT reading: if --title stopped overriding, or were overwritten once Claude titles the conversation, or if the `--` separator were dropped from the CLI. Untested: whether the title survives a saggar restart or a `--session-id` terminal reuse. Also unproven in the override direction — a conversation title is not stored on disk anywhere readable (~/.claude/history.jsonl holds prompts, not titles), so it could not be confirmed that the 42-turn probe ever generated one for --title to beat. What is measured is that the explicit name held where the default did not, and that the flag is on the wire.",
        "ledger": "Anything game_loop passes to `saggar agent` names the saggar terminal",
        "exercisable": "yes",
        "exercisable_why": "EXERCISED 2026-08-25 by the act the earlier entry said would be needed: a live handover through the running app, with the terminal's name read back from `saggar list --json`. It costs a real claude session, so it is a thing a person re-runs, never a gate",
        "exercised_by": "`saggar agent claude --title \"<name>\" -- <task>`, then `saggar list --json` for the terminal's name",
        "verified_on": "2026-08-27",
        "verified_against": "`saggar --help` at ~/.local/bin/saggar on 2026-08-27; four live agent terminals on 2026-08-26; the installed presence hook at ~/.saggar/claude-presence-hook.sh (rewritten 2026-08-26 11:25)",
        "verified_how": "RE-READ 2026-08-27 off the CLI on this machine's PATH, which the 2026-08-26 reading could not reach: `saggar agent <agent> <task…>` with `--title <title>` and `--` documented as ending agent options before the task. The live four-terminal exercise behind the naming behaviour is the 2026-08-26 one and was NOT re-run here — what 2026-08-27 adds is the flag's presence and the fact that game_loop now sends it.",
        "exercise_note": "the falsifying case is constructible and was constructed: a prompt whose subject is distinctive. If a name ever came from the caller, the same handover with --title set would show it",
        "superseded_note": "The 2026-08-25 finding that Claude's own conversation title names the terminal is NOT confirmed live any more either: ~/.saggar/chat-info/ has not been written since 2026-08-24, so the status-line bridge that carried it is no longer the path saggar uses. Long-lived sessions still show conversation titles in `saggar list`, so some path still exists; which one was not traced."
      }
    ]
  }
}
