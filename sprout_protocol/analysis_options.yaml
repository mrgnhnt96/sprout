include: package:lints/recommended.yaml

analyzer:
  language:
    strict-casts: true
    strict-inference: true
    strict-raw-types: true
  errors:
    # The three checks run `dart analyze --fatal-infos --fatal-warnings`, so
    # every diagnostic already fails the build. Nothing is downgraded here.
    todo: ignore
